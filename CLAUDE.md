# CLAUDE.md

This repository contains the OpenClaw DingTalk channel plugin.

- Source code lives under `src/`.
- The plugin entry point is `index.ts`.
- Configuration examples are in `config.example.yaml`.
